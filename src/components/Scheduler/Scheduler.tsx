import React from 'react';

import TextField from '@mui/material/TextField/TextField';

export interface SchedulerProps {}

const Scheduler: React.FC<SchedulerProps> = (p) => {
  return (
   <div>
        <TextField label={'Kristi'} />
   </div>
  );
}
export default Scheduler;
